import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    position: "relative",
  },
  firstIcon: {
    display: "block",
  },
  secondIcon: {
    position: "absolute",
    bottom: 13,
    left: 13,
  },
});

export default styles;
