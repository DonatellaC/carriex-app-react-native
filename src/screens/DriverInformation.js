import { SafeAreaView, ScrollView } from "react-native";
import View from "../features/DriverInformation";

export default function DriverInformation() {
  return (
    <SafeAreaView>
      <ScrollView>
        <View />
      </ScrollView>
    </SafeAreaView>
  );
}
