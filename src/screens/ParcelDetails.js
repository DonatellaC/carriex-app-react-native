import { SafeAreaView, ScrollView } from "react-native";
import View from "../features/ParcelDetails";

export default function ParcelDetails() {
  return (
    <SafeAreaView>
      <ScrollView>
        <View />
      </ScrollView>
    </SafeAreaView>
  );
}
