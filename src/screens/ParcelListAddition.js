import { SafeAreaView, ScrollView } from "react-native";
import View from "../features/ParcelListAddition";

export default function ParcelListAddition() {
  return (
    <SafeAreaView>
      <ScrollView>
        <View />
      </ScrollView>
    </SafeAreaView>
  );
}
