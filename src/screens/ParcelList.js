import { SafeAreaView, ScrollView } from "react-native";
import View from "../features/ParcelList";

export default function ParcelList() {
  return (
    <SafeAreaView>
      <ScrollView>
        <View />
      </ScrollView>
    </SafeAreaView>
  );
}
