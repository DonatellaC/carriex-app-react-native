import { SafeAreaView, ScrollView } from "react-native";
import View from "../features/CarrierParcelList";

export default function CarrierParcelList() {
  return (
    <SafeAreaView>
      <ScrollView>
        <View />
      </ScrollView>
    </SafeAreaView>
  );
}
